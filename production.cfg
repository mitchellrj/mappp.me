[buildout]
extends = base.cfg
directory = /var/www-mappp
parts += wurfl-cron

[pyramid]
host = 127.0.0.1
executable -= py

[wurfl-cron]
recipe = z3c.recipe.crontab
times = 0 * * * *
command = su -m mappp -c "${buildout:bin-directory}/wurfl-update > /dev/null 2> /dev/null || echo 'FAILED'"
